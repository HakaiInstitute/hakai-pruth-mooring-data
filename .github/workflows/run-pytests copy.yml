name: Test dataset

on:
  push:
    branches: production

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_ACCESS_TOKEN }}
          repository: HakaiInstitute/hakai-sensor-network-sa-sensors-python-only
          event-type: my-event
          client-payload: 'oh yeah!'
